name: CI Pipeline - ck-mcp-server → GHCR Patch Release

on:
  workflow_dispatch:
  repository_dispatch:
    types: [trigger-build-workflow]

permissions:
  contents: write
  packages: write

env:
  DOCKER_IMAGE_PATH: ghcr.io/ckgitrepouser/prod/ck-mcp-server/ck-mcp-server
jobs:
  build-push-verify:
    runs-on: ubuntu-22.04

    env:
      DOCKER_BUILDKIT: 1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # -------------------------------
      # Fetch latest GitHub release tag
      # -------------------------------
      # - name: Fetch latest GitHub release tag
      #   id: get-latest-tag
      #   env:
          # GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   run: |
      #     set -e

      #     LATEST_TAG=$(gh release list \
      #       --limit 1 \
      #       --json tagName \
      #       --jq '.[0].tagName')

      #     if [[ -z "$LATEST_TAG" ]]; then
      #       echo "❌ No release tag found"
      #       exit 1
      #     fi

      #     if [[ ! "$LATEST_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
      #       echo "❌ Invalid semantic version: $LATEST_TAG"
      #       exit 1
      #     fi

      #     echo "latest_tag=$LATEST_TAG" >> $GITHUB_OUTPUT
      
      - name: Bump patch version
        id: version
        uses: firgga-sunil/poc-app-of-apps/.github/actions/fetch-and-increment-version@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}   

      # -------------------------------
      # Increment patch version
      # -------------------------------
      # - name: Increment patch version
      #   id: bump-version
      #   run: |
      #     set -e

      #     VERSION="${{ steps.get-latest-tag.outputs.latest_tag }}"
      #     VERSION="${VERSION#v}"

      #     IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"
      #     PATCH=$((PATCH + 1))

      #     NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"

      #     echo "new_tag=$NEW_TAG" >> $GITHUB_OUTPUT
      #     echo "✅ New tag: $NEW_TAG"

      # -------------------------------
      # Docker multi-arch build setup
      # -------------------------------
      # - name: Set up QEMU
      #   uses: docker/setup-qemu-action@v3

      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v3

      # - name: Login to GHCR
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ghcr.io
      #     username: ${{ secrets.GPR_USER }}
      #     password: ${{ secrets.GPR_TOKEN }}

      # # -------------------------------
      # # Build & push Docker image
      # # -------------------------------
      # - name: Build and push multi-platform Docker image
      #   run: |
      #     set -e

      #     TAG="${{ steps.bump-version.outputs.new_tag }}"
      #     IMAGE="${{ env.DOCKER_IMAGE_PATH }}"

      #     echo "🔨 Building multi-arch image: $IMAGE:$TAG"

      #     docker buildx build \
      #       --platform linux/amd64,linux/arm64 \
      #       --push \
      #       --tag "$IMAGE:$TAG" \
      #       --tag "$IMAGE:latest" \
      #       .

      #     echo "✅ Image pushed successfully"
      - name: Build and push Docker image
        uses: firgga-sunil/poc-app-of-apps/.github/actions/docker-build-push@main
        with:
          image: ${{ env.DOCKER_IMAGE_PATH }}
          tag: ${{ steps.version.outputs.new_tag }}
          username: ${{ secrets.GPR_USER }}
          password: ${{ secrets.GPR_TOKEN }}

      # -------------------------------
      # Verify image exists in GHCR
      # -------------------------------

      # - name: Verify image exists in GHCR
      #   run: |
      #     set -e

      #     TAG="${{ steps.bump-version.outputs.new_tag }}"
      #     IMAGE="${{ env.DOCKER_IMAGE_PATH }}"

      #     if docker manifest inspect "$IMAGE:$TAG" > /dev/null 2>&1; then
      #       echo "✅ Image exists in GHCR"
      #       docker manifest inspect "$IMAGE:$TAG"
      #     else
      #       echo "❌ Image not found in GHCR"
      #       exit 1
      #     fi

      - name: Verify image exists in GHCR
        uses: firgga-sunil/poc-app-of-apps/.github/actions/verify-image-upload@main
        with:
          image: ${{ env.DOCKER_IMAGE_PATH }}
          tag: ${{ steps.version.outputs.new_tag }}  
      # -------------------------------
      # Create GitHub Release
      # -------------------------------
      # - name: Create GitHub Release
      #   env:
      #     GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   run: |
      #     set -e

      #     TAG="${{ steps.bump-version.outputs.new_tag }}"
      #     PREV_TAG="${{ steps.get-latest-tag.outputs.latest_tag }}"
      #     IMAGE="${{ env.DOCKER_IMAGE_PATH }}"

      #     RELEASE_NOTES=$(cat <<EOF
      #     ## 🚀 Release ${TAG}

      #     ### Docker Image
      #     \`\`\`
      #     docker pull ${IMAGE}:${TAG}
      #     \`\`\`

      #     ### Supported Architectures
      #     - linux/amd64
      #     - linux/arm64

      #     ### Changes
      #     - Automated patch release from ${PREV_TAG} → ${TAG}
      #     - Multi-platform Docker image published to GHCR

      #     ### Image Tags
      #     - \`${IMAGE}:${TAG}\`
      #     - \`${IMAGE}:latest\`
      #     EOF
      #     )

      #     gh release create "$TAG" \
      #       --title "Release $TAG" \
      #       --notes "$RELEASE_NOTES" \
      #       --latest

      #     echo "✅ GitHub release created: $TAG"
      - name: Create GitHub Release
        uses: firgga-sunil/poc-app-of-apps/.github/actions/create-github-release@main
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          new_tag: ${{ steps.version.outputs.new_tag }}
          latest_tag: ${{ steps.version.outputs.latest_tag }}
          image: ${{ env.DOCKER_IMAGE_PATH }}
      # -------------------------------
      # Summary
      # -------------------------------
      - name: Summary
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" | tee -a $GITHUB_STEP_SUMMARY
          echo "✅ PIPELINE COMPLETED" | tee -a $GITHUB_STEP_SUMMARY
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━" | tee -a $GITHUB_STEP_SUMMARY
          echo "" | tee -a $GITHUB_STEP_SUMMARY
          echo "**Previous Tag:** ${{ steps.get-latest-tag.outputs.latest_tag }}" | tee -a $GITHUB_STEP_SUMMARY
          echo "**New Tag:** ${{ steps.bump-version.outputs.new_tag }}" | tee -a $GITHUB_STEP_SUMMARY
          echo "**Image:** \`${{ env.DOCKER_IMAGE_PATH }}:${{ steps.bump-version.outputs.new_tag }}\`" | tee -a $GITHUB_STEP_SUMMARY
          echo "**Platforms:** linux/amd64, linux/arm64" | tee -a $GITHUB_STEP_SUMMARY
